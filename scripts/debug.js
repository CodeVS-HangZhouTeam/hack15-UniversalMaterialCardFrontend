requirejs(["cardloader"], function(cardloader) {
    cardloader.loadCard(".page-content", "card", {});
    cardloader.loadCard(".page-content", "card", {});
    cardloader.loadCard(".page-content", "card", {});
    cardloader.loadCard(".page-content", "card", {});
    cardloader.loadCard(".page-content", "card", {});
    cardloader.loadCard(".page-content", "card", {});
});